#include <iostream>
//#include "DynamicArray.h"
using namespace std;

int main() {
	const size_t valueMaxCount = 1000;
	cout << "dynamicArray.getCapacity()" << endl;
	/*DynamicArray<int> dynamicArray;
	dynamicArray.reserve(valueMaxCount);
	
	cout << dynamicArray.getCapacity() << endl;

	for (int i = 0; i < valueMaxCount; i++) {
		dynamicArray.push_back(i);
	}

	cout << dynamicArray.getCapacity() << endl << dynamicArray.getSize();*/

	return 0;
}