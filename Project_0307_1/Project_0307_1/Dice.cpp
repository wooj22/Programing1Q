#include <iostream>
#include <cstdlib>
#include <ctime>
#include <stdio.h>
#pragma warning(disable:4996)
using namespace std;
/*
void diceStart() {
    int input;
    int num;

    //cout << "정수를 입력해주세요 :  ";
    printf("정수를 입력해주세요 : ");

    //cin >> input;
    scanf("%d", &input);

    num = rand() % input + 1;

    cout << "주사위 결과: " << num << endl;
    printf("주사위 결과 : " + num + '\n');
}

int main() {
    srand(static_cast<unsigned int>(time(NULL)));

    for (int i = 0; i < 10; i++) {
        diceStart();
    }

    return 0;
}
*/